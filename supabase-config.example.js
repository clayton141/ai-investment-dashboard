// Copy to supabase-config.js after the Supabase project is provisioned.
// The anon key is intentionally safe to expose in the browser when RLS is configured correctly.
window.SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_ANON_KEY";
